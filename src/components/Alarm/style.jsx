import styled from "styled-components";

const StyleAlarm = styled.div`
  padding: 4rem;

  .rs-modal-body {
    font-size: 3.5rem;
    text-align: center;
    line-height: 4.5rem;
    font-weight: var(--fw-medium);
    color: var(--color-grey-0);
  }
`;

export default StyleAlarm;
